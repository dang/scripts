# vim: ft=sh:
# Setup for arch

me=$(readlink --canonicalize --no-newline $BASH_SOURCE)

source "${SCRIPTDIR}/functions.sh"

WANTPROGS="${WANTPROGS} pigz"

if [ -n "${SCRIPTS_SETUP}" ]; then
	# One-time setup operations here
	;
fi
