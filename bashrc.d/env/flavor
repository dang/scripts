# vim: ft=sh:
# Detect the linux flavor

FLAVOR="unknown"

if hash lsb_release 2>/dev/null; then
	f=$(lsb_release -is)
	FLAVOR=${f,,}
elif [ -f "/etc/os-release" ]; then
	. /etc/os-release
	f="${ID}"
	FLAVOR=${f,,}
fi

export FLAVOR
