# vim: ft=sh:
# Detect the linux flavor

FLAVOR="unknown"

if hash lsb_release 2>/dev/null; then
	FLAVOR=$(lsb_release -is)
elif [ -f "/etc/os-release" ]; then
	. /etc/os-release
	FLAVOR="${ID}"
fi

export FLAVOR
