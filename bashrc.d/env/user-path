# vim: ft=sh:
# set PATH so it includes user's private bin if it exists
pathprepend "${HOME}/bin"

# set PATH for the flavor
pathprepend "${SCRIPTS}/flavor/${FLAVOR}"

# Add GTWS to path
pathappend "${SCRIPTS}/gtws"
